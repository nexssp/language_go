// additional info for templates like copy extra libraries.
const config = {
  files: [],
  commands: ["go install github.com/json-iterator/go"],
  repos: [],
  descriptions: []
};

module.exports = config;
